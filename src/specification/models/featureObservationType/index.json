{
  "additionalProperties": false,
  "properties": {
    "featureObservationTypeName": {
      "type": "string",
      "enum": ["sex", "length", "age", "weight", "conditionAtCollecting", "ageStage"]
    },
    "id": { "type": "integer", "enum": [1, 2, 3, 4, 5, 6] }
  },
  "required":["featureObservationTypeName"]
}

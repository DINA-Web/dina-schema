{
  "additionalProperties": false,
  "properties": {
    "featureObservationTypeName": {
      "type": "string",
      "enum": ["age", "ageStage", "conditionAtCollecting", "sex"]
    },
    "id": { "type": "integer", "enum": [1, 2, 3, 4] }
  },
  "required":["featureObservationTypeName"]
}

{
  "additionalProperties": false,
  "properties": {
    "id": { "type": "integer" },
    "featureObservationTypeName": {
      "type": "string",
      "enum": ["age", "ageStage", "sex"]
    }
  },
  "required": ["id", "featureObservationTypeName"]
}
